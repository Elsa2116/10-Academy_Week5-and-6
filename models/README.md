# Models Directory

This folder stores trained model artifacts created by the training pipeline.

Keep serialized models and related outputs here, such as joblib or pickle files produced by `scripts/train_models.py`.

The repository keeps this folder in version control with placeholder documentation so the expected output location is always visible.
